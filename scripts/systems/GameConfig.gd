extends Node
## Глобальный выбор партии (autoload). Заполняется из меню/настройки партии.

var variant := "copenhagen"    # "brandubh" | "tablut" | "fetlar" | "copenhagen"
var human_side := "defenders"  # "attackers" | "defenders"
var difficulty := "normal"     # уровень ИИ (расширим после анализа)
